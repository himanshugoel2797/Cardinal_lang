module emitc

# Emit C for the Cardinal program named on argv[0]; print to stdout.

import io
import fs
import sys
import codegen

func main () -> i32
    let a = (sys::args)
    do (io::print (codegen::emit_c (fs::read_file a[0])))
    return 0i32
end

module test_array_of_vec

import io

func main () -> i32
    let v1 (vec i32) = (vec i32)
    do (push v1 1i32)
    do (push v1 2i32)
    let v2 (vec i32) = (vec i32)
    do (push v2 3i32)
    let arr [(vec i32)] = [v1 v2]
    do (io::println (to_str arr))
    return 0i32
end

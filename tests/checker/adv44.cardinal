module adv44
# ADVERSARIAL: for-numeric with i64 bounds and i32 index conflict
# for i = 0i64 to 10i64 -- index defaults to i32, so this is a type conflict
func f () -> i32
    let sum = 0i32
    for i = 0i64 to 10i64
        set sum = (+ sum i)
    end
    return sum
end
